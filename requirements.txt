requests>=2.31.0
python-dotenv>=1.0.0
edge-tts>=6.1.0
PyYAML>=6.0
Pillow>=10.0.0
manim>=0.19.0
# NOTE: manim requires system deps first: sudo apt install libcairo2-dev pkg-config
# (and fonts: sudo apt install fonts-dejavu-core) before pip install manim.
